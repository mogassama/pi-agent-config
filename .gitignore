# Secrets
auth.json
.env
.env.*

# Local runtime
sessions/
logs/
tmp/
.cache/
.pi-subagents/
cc-cli-logs/
run-history.jsonl
# journal.md est la SORTIE de pi-session-journal ; l'extension, elle, est
# versionnée depuis 2026-09-08 — elle fait partie de la configuration lancée.
journal.md

# Local settings
models.json

# Node / TypeScript
node_modules/
dist/
build/

# macOS
.DS_Store

*.log
*.bak


.pi/
models-store.json
evidence/
.pi-subagent-runs/
.backup-*/

# pi-secret-gate est une extension, pas un secret. Le .gitignore global
# exclut *secret*, ce qui a rendu ce répertoire invisible pendant une semaine.
!extensions/pi-secret-gate/
!extensions/pi-secret-gate/**
